% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_segmentation.R
\name{plot_segmentation}
\alias{plot_segmentation}
\title{Plot Segmentation Results}
\usage{
plot_segmentation(data, changepoints, title = "Segmentation Plot")
}
\arguments{
\item{data}{A numeric vector representing the time series data.}

\item{changepoints}{An integer vector of detected changepoint indices.}

\item{title}{A character string for the plot title. Defaults to "Segmentation Plot".}
}
\value{
This function returns a base R plot as a side effect, with:
\itemize{
  \item the original data in blue,
  \item vertical dashed red lines at each changepoint,
  \item green horizontal lines representing the segment-wise mean.
}
It does not return a value.
}
\description{
Visualizes the result of a segmentation algorithm by plotting the time series,
vertical lines at detected changepoints, and horizontal segment means.
}
\examples{
n <- 100
data <- rep(c(0, 5, 2.5, 7), each = n) + rnorm(4 * n)
changepoints <- c(n, 2 * n, 3 * n, length(data))
plot_segmentation(data, changepoints, title = "Example Segmentation")

}
